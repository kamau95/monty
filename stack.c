#include "monty.h"
/**
 * f_stack - prints the top element ot the stack
 * @head: stack head pointer
 * @counter: line_number
 * Return: no return/ null
*/
void f_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
